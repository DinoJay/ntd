if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
    //var API_URL = "http://localhost:3030/StarWars/query";
    //var query = "select+*+where+{%3Fx+%3Fd+%3Fx}";

    //var result = HTTP.call("GET", "http://api.twitter.com/xyz",
      //{params: {query: query}});
  });
}
